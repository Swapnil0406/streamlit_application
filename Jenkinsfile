pipeline {
    agent any
    
    stages {
        stage('Git repo pull') {
            steps {
                script {
                    // Creating a directory
                   git 'https://github.com/Swapnil0406/streamlit_application.git'
                }
            }
          }
        }
 }
